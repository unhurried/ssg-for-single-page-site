---
name: commit-message
description: Formatting rules for git commit messages in this repo. Use whenever creating a git commit (git commit) in this project, so the message format doesn't need to be specified each time.
---

# Commit message format

When running `git commit` in this repository:

- Write a single concise line (no body, no bullet points) unless the user explicitly asks for more detail.
- Use imperative mood (e.g. "Fix", "Add", "Remove"), matching this repo's existing commit log (`git log --oneline`).
- Do NOT add a `Co-Authored-By` trailer.
- Do NOT add an AI-generated-content notice or similar footer.
