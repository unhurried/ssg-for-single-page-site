---
// Top page of the default locale (/). To add a locale, add src/pages/<locale>/index.astro.
import DocumentIndex from '../components/DocumentIndex.astro';
import { defaultLocale } from '../i18n';
---

<DocumentIndex locale={defaultLocale} />
